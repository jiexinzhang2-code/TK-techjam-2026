# Optional alternative to the first-run interactive configuration.
# Set only the provider you plan to use; never commit a real key.
KUAI_AGENT_LLM_PROVIDER=openai
OPENAI_API_KEY=
# DEEPSEEK_API_KEY=
KUAI_AGENT_LLM_MODEL=gpt-5.6-sol
